version: '3'

services:
  ubuntu:
    image: accetto/ubuntu-vnc-xfce-g3:latest
    environment:
      - NOVNC_HEARTBEAT=5
    ports:
      - 6901:6901
    cap_add:
      #- ALL          # capability may not be added
      #- SYS_ADMIN    # capability may not be added
      #- SYS_CHROOT   # capability may not be added
      #- CHOWN        # capability may not be added
      #- SETPCAP      # allowed
      #- SETUID       # capability may not be added
      #- SETFCAP      # capability may not be added
      #- SETGID       # capability may not be added
      #- FOWNER       # capability may not be added
      #- DAC_OVERRIDE # capability may not be added
      #- FSETID       # capability may not be added
      - MKNOD
    #user: 0
    
